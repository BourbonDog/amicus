# v4.7 Docs Pass — Implementation Plan

> ⚠️ **HISTORICAL RECORD — do NOT quote this document's prose as documentation.** Several
> prescriptions below were found **false** during execution and were corrected in the branch's own
> commits. Known-wrong text still present here, kept so the record of what went wrong survives:
> "suppresses the launch banner" (no launch banner exists — `--quiet` gates preflight errors, the
> wave-result summary, and per-leg ticks); "there is no `--no-cost-gate` equivalent over MCP"
> (`amicus_council_run` has one); "a permanent no-op" (the plans-index guard is a runtime
> `existsSync`, so it can reactivate). The shipped truth is whatever `docs/` says at HEAD, not what
> this plan told an implementer to write. A reviewer diffing this PR will see these strings — they
> are inputs, not output.
>
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the last v4.7 ROADMAP scope line ("README + docs update") by fixing the sentences
that v4.7 made **false** and documenting the two behaviours a user will actually trip over.

**Architecture:** Six tasks, grouped by file so a reviewer can accept or reject each independently.
All hand-written prose plus **one** source edit (a `--quiet` line in `src/cli.js`'s fanout help
block, owner-ruled 2026-08-08). No generated content is touched.

**Tech Stack:** Markdown. The gates are jest tests plus `scripts/validate-docs.js` and
`scripts/generate-docs.js`.

---

## Global Constraints

Every task's requirements implicitly include this section.

**Baseline.** Branch `docs/v4.7-readme-and-docs`, worktree `C:\Users\sendt\code\amicus-wt-v47-docs`,
branched from `b365e03` (= `origin/main`, the v4.7 PR7 merge). The suite was measured green at that
commit: **507 suites / 6883 passed, 8 skipped**. `package.json` is **4.6.3** and stays that way —
the version bump is the release cut's job, not this PR's.

**This is scoped as an "S".** ~110–150 net lines across eight files. A bloated docs PR delays the
release for prose nobody asked for. If a task tempts you to rewrite a section, don't — fix the false
sentence and move on.

### What is machine-generated (never hand-edit)

`scripts/generate-docs.js` writes exactly two blocks, **both in `CLAUDE.md`**:
`<!-- AUTO:tree -->` and `<!-- AUTO:modules -->`, fed by
`TREE_DIRS = ['bin/','src/','electron/','scripts/','evals/']`.

**Nothing under `docs/` is generated.** Every file this plan touches is hand-editable. The
`<!-- AUTO:… -->` strings inside `docs/doc-system.md` are *fenced examples*, not live markers —
Task 5 edits that file's prose and must not be confused by them.

### The doc gates — what will and will not catch you

| Gate | What it pins |
|---|---|
| `tests/docs-quick-sync.test.js` | The `"version": "4.6.3"` examples in `README.md:453` and `docs/usage.md:509` (regex-pinned to `package.json`), the fanout `--council` wording, the shared per-leg knobs, `--max-cost`/`--no-cost-gate`, the `list --status` value set, the OpenRouter-402 section. |
| `tests/docs-command-coverage.test.js` | Tool *names* scraped live from `src/mcp-tools.js`; 5 literal command names in README, 4 in usage.md; `amicus doctor` must precede `## Auth / 401` in troubleshooting.md. |
| `tests/council-reference-docs.test.js` | Every **top-level** key of a live `tally()`/`buildVerdict()` record must appear in `docs/council.md`; every ` ```json ` fence there must parse. |
| `tests/docs-council-toc-anchors.test.js` | Re-implements GitHub's slugger over every `](#…)` in `docs/council.md`. **Any anchor you add or rename must resolve.** |
| `tests/where-things-live-docs.test.js` | `config.json`'s top-level keys as exactly `default`, `aliases`, `councils`, `providers`, `routing`; session-storage tables only at `/wave/i` and `/leg/i` granularity. |
| `tests/model-notes-shipped-docs.test.js` | Section headings and three global rules in MODEL-NOTES.md. Contains **no** `judge`/`runStats` assertion. |

**`docs/ROADMAP.md` and `docs/doc-system.md` are pinned by NOTHING** (`grep -rln ROADMAP tests/` →
zero files). That is exactly why both contain live falsehoods. Nothing will catch a regression there
but a human.

**No test executes `--help`.** Zero CLI flags are pinned to CLI reality anywhere.

### Commands

- Targeted tests: `npx jest tests/docs-quick-sync.test.js tests/docs-command-coverage.test.js tests/where-things-live-docs.test.js tests/docs-council-toc-anchors.test.js tests/council-reference-docs.test.js tests/model-notes-shipped-docs.test.js`
- `node scripts/validate-docs.js --full` → must print "All markers are current." and exit 0.
- `node scripts/generate-docs.js --check` → exit 0.
- Never bare `npm install`. Never `git push`. Never a PATH `amicus` — use `node bin/amicus.js`.

### Explicitly OUT of scope — do not do these

- **The `"version": "4.6.3"` examples** in `README.md:453` and `docs/usage.md:509`. They are pinned
  to `package.json` and are correct *today*. They get updated in the release-cut commit, together
  with the bump, or the suite goes red in between.
- **The ROADMAP `— ✅ SHIPPED v4.7.0` marker.** v4.7 has not shipped at this commit; adding it now
  would make the file lie in the other direction. Release cut owns it. (Task 5 fixes only what is
  false *today*.)
- Anything in the recon report's FILE list (F-1…F-6). Those are test-code and hygiene items and
  turn this into an M.

### Plan-authoring failure modes — if you spot one IN THIS PLAN, stop and report it

Every line number below was re-read at `b365e03` immediately before this plan was written. They will
still drift if an earlier task inserts lines above them. **Locate every edit by quoted content, not
by line number**, and if a quoted string is not where the plan says, report it rather than guessing.

---

## Task 1: `docs/usage.md` — tag non-inheritance, two flag corrections, and `--quiet`

**Files:**
- Modify: `docs/usage.md` (913 lines)
- Modify: `src/cli.js` (the `Options for 'fanout':` help block, currently starting at `:465`)

**Interfaces:** none — no other task depends on this one.

- [ ] **Step 1: MUST-1 — say that a tag is not inherited**

The `--tag` closing note lives in the `amicus list` flags paragraph. Find it by this exact quoted
text (verified present): `Tag itself is set at launch with \`--tag <t>\` on`.

Extend that note to state the limitation. The facts, each verified:
- `amicus continue` and `amicus resume` write their spend rows with **no tag** —
  `src/sidecar/continue.js`, `src/sidecar/resume.js` and `src/cli-handlers-resume-continue.js`
  contain **zero** `tag` references.
- `src/utils/spend-ledger.js:94` writes `tag: tag || null`; `src/spend-query.js:58` maps a null tag
  to `(unattributed)`.
- A `fanout --retry-failed` wave does not carry the original wave's tag, and `--tag` combined with
  `--retry-failed` is rejected with `BAD_ARGS`.

So those rows group under `(unattributed)` in `spend --group-by tag`. Frame it as a **known
limitation**, in the same voice this file already uses for the `{{input}}` limitation (~`:260`) —
not as a bug report.

**Wording caution:** describe the mechanism you can verify (no tag is written → the row groups
under `(unattributed)`). Do not claim a specific observed ledger output; nobody has run a real
tagged-then-continued session end to end.

- [ ] **Step 2: S-1 — `models --check --strict`**

`amicus models --check --strict` is live in `node bin/amicus.js --help` but appears in no doc. The
drift paragraph in the `amicus models` section currently says drift "never changes the exit code",
which is **actively misleading** without `--strict` — that flag is precisely what makes curated
per-gateway drift exit non-zero. Add it to the fenced example and add one sentence beside the drift
paragraph. Run `node bin/amicus.js models --help` first and use its own wording.

- [ ] **Step 3: S-2 — `council run --out-dir` now refuses paths outside the project**

One clause on the existing `--out-dir <dir>` option row. The error message is self-describing, so no
troubleshooting section is needed.

- [ ] **Step 4: `fanout --quiet` — docs row AND help line (owner ruling 2026-08-08)**

The flag works (`src/cli-handlers-fanout.js:157-159` forwards it; `src/sidecar/fanout.js` honours it
at `:75`, `:103`, `:248`) and is documented nowhere — and `node bin/amicus.js fanout --help` does not
list it either. Add both:
- a `--quiet` row in usage.md's fanout Key-options table;
- a matching line in `src/cli.js`'s `Options for 'fanout':` block, matching the surrounding
  two-column alignment exactly.

⚠️ **`tests/docs-quick-sync.test.js` pins parts of the fanout options wording** (the `--council`
wording, the shared per-leg knobs, `--max-cost`/`--no-cost-gate`). Run that test after editing
`src/cli.js` and usage.md, and if it fails, report which assertion — do not "fix" the test.

⚠️ **Wording caution (recon UNVERIFIED item):** nobody has run a real wave to watch the banner not
print. Say "suppresses the launch banner and per-leg progress output" — do **not** write an
absolute claim about exactly which lines vanish.

- [ ] **Step 5: Verify**

```
npx jest tests/docs-quick-sync.test.js tests/docs-command-coverage.test.js
node bin/amicus.js fanout --help
```
The last command must now show `--quiet`. Also run `node scripts/validate-docs.js --full` (exit 0)
and `npx eslint src/cli.js` (exit 0).

- [ ] **Step 6: Commit**

```bash
git add docs/usage.md src/cli.js
git commit -m "docs(usage): tag non-inheritance, --strict, --out-dir fencing, and fanout --quiet"
```

---

## Task 2: `docs/configuration.md` + `docs/troubleshooting.md` — the cost gate and two false claims

**Files:**
- Modify: `docs/configuration.md` (417 lines)
- Modify: `docs/troubleshooting.md` (327 lines)

- [ ] **Step 1: MUST-2a — document the cost-gate config keys**

`grep -rn maxCostPerMtok docs/*.md README.md skills/` returns **zero hits** (verified). Add a short
**"Cost gate"** subsection to `docs/configuration.md`, adjacent to the commented `config.json`
example (that example runs roughly `:311-367` and enumerates
`default`/`aliases`/`councils`/`routing`/`providers`/`workspace`).

Facts, each verified in source:
- `maxCostPerMtok` — hard per-$/Mtok refusal threshold, **default 60** (`src/sidecar/budget.js:22`).
  A non-positive value falls back to the default.
- `maxCost` — soft total-$ estimate ceiling; absent means no ceiling.
- Both are top-level, **hand-edited only** — no wizard or CLI command writes them.
- `--max-cost` overrides `maxCost` per call; `--no-cost-gate` disables both guards, **CLI only**.

⚠️ `tests/where-things-live-docs.test.js` pins `config.json`'s top-level keys as exactly `default`,
`aliases`, `councils`, `providers`, `routing`. Documenting two more keys does **not** violate it
(the assertion is about that specific list, not a closed set) — but run that test and confirm.

- [ ] **Step 2: MUST-2b — a troubleshooting section for the refusal**

Add `## MCP run fails with "budget gate refused the run"` to `docs/troubleshooting.md`. Four to six
lines. What to say:
- v4.7 made the `amicus_start` shared-server gate **unconditional** (`src/mcp-server.js:442-467`),
  so a no-pack MCP start that worked on 4.6.x can now refuse.
- The two guards are independent, and when **both** fire, raising one will not clear the run.
- The fix is raising `maxCostPerMtok`/`maxCost` in the config (link to the new subsection) or
  choosing a cheaper model.
- **There is no `--no-cost-gate` equivalent over MCP.**

⚠️ `tests/docs-command-coverage.test.js` requires `amicus doctor` to appear **before**
`## Auth / 401` in this file. Place your new section so that ordering still holds, and run the test.

- [ ] **Step 3: MUST-4 — the wave-directory clause is false**

In `docs/configuration.md` § Session storage, the **"Fanout waves."** paragraph ends with this
clause (verified verbatim):

> nothing wave-specific is stored beyond the per-leg session dirs themselves plus the wave's own
> `metadata.json` (type `wave`, `legs: [...]`).

That is false, and **not only for MCP waves** — `src/sidecar/fanout.js:145` writes `briefing.md` on
the CLI path too, so the clause has always been wrong. Keep the per-leg-dir description; replace the
clause with what a wave dir actually holds:
- `metadata.json` (type `wave`, `legs: [...]`, plus a 200-char rendered `briefing` excerpt);
- `wave.json` on completion;
- **`briefing.md`** — written on **both** paths, holding the **rendered** prompt. This is the corpus
  `amicus list --search` matches against;
- **`briefing-input.md`** — only for an `amicus_fanout` wave whose prompt came from a template: the
  raw pre-render prompt handed to the spawned child, so the child's own re-render stays
  byte-identical and `promptMeta.template` provenance survives.

Precedent for the shape: `docs/council.md:213-236` already enumerates a council run dir and calls out
its MCP-only extra files. Match that style.

- [ ] **Step 4: MUST-7 — the doctor check list is missing a check**

`docs/troubleshooting.md:8` claims to list what `amicus doctor` "checks, in order" and says
`session index tmp files`. There are **two** separate checks: `src/cli-handlers-doctor.js:206`
pushes `sessions-index-tmp` ("Session index tmp files") and `:208` pushes `session-metadata-tmp`
("Session metadata tmp files"). Change the phrase to `session index and session metadata tmp files`.

`docs/configuration.md:247-254` already describes both sweeps, so the two files currently disagree.

- [ ] **Step 5: Verify and commit**

```
npx jest tests/where-things-live-docs.test.js tests/docs-command-coverage.test.js tests/docs-quick-sync.test.js
node scripts/validate-docs.js --full
```

```bash
git add docs/configuration.md docs/troubleshooting.md
git commit -m "docs: document the cost gate, correct the wave-dir contents and the doctor check list"
```

---

## Task 3: `docs/council.md` — the `runStats[].role` roster

**Files:**
- Modify: `docs/council.md` (1025 lines)

- [ ] **Step 1: MUST-3 — add `rebuttal`/`revote` AND scope the two trailing claims**

Find the section by its heading text: `**\`runStats[].role\` roster (v4.7 row-per-launch).**`
(around `:557`). Its *Non-primary rows* paragraph currently lists `chair-attempt`, `repair`,
`superseded` and ends with two **blanket** claims:

> These still cost money and appear in the `council report`/`council tally` cost tables (suffixed
> the same way judge rows are), but the Workspace seats panel filters them out — they aren't seats.

Add `rebuttal` and `revote` (v4.1 `--debate` roles that pre-date row-per-launch;
`src/council/debate.js:22` `DEBATE_ROLES = new Set(['rebuttal','revote'])`, appended onto
`debatedInput.runStats` by `run-debate.js:227-234`).

**Then scope both trailing claims, or adding the two roles makes them false:**
- **Cost-table suffixing** applies to `judge` / `chair-attempt` / `repair` / `superseded` **only** —
  verified at `src/council/report.js:80-84`, where `ROLE_SUFFIX` has exactly those four keys.
  `rebuttal`/`revote` get **no** suffix.
- **The Workspace seats-panel exclusion set** is `chair-attempt` / `repair` / `superseded` **only** —
  verified at `electron/workspace-ui/live-model.js:108-111`. `rebuttal`/`revote` are **not** filtered.

Leave the "three **new** non-primary roles" count in the ledger-join paragraph (~`:583-584`) alone —
that count is about v4.7's additions and is still correct.

There is a standing in-tree request for exactly this: `electron/workspace-ui/live-model.js:119-123`
carries an F37 DE-ROT note asking for it to be stated explicitly.

- [ ] **Step 2: S-3 — say what the trailing flag cell means**

The Run-detail bullet (~`:330-331`) mentions "a stall flag". Extend it to describe the cell as it now
behaves: `⏳ stalled` while live, `↻ retried once` on a finished run marking a reviewing seat whose
once-only Stage-1 retry did not save it (`electron/workspace-ui/workspace-seats.js:118-124`). One
sentence — this is the only place a user can learn what that glyph means.

- [ ] **Step 3: Verify and commit**

⚠️ Two gates read this file. `tests/council-reference-docs.test.js` requires every top-level tally /
verdict key to be present and every ` ```json ` fence to parse. `tests/docs-council-toc-anchors.test.js`
re-implements GitHub's slugger over every `](#…)` link — **if you add or rename a heading, its
anchor must resolve.**

```
npx jest tests/council-reference-docs.test.js tests/docs-council-toc-anchors.test.js
```

```bash
git add docs/council.md
git commit -m "docs(council): complete the runStats role roster and scope its suffix/filter claims"
```

---

## Task 4: Two small files still teaching pre-v4.7 rules

**Files:**
- Modify: `skills/second-opinion/MODEL-NOTES.md`
- Modify: `docs/doc-system.md` (92 lines)

- [ ] **Step 1: MUST-5 — MODEL-NOTES.md is the last file on the old judge-exclusion rule**

Find this text (verified verbatim, ~`:104-107`):

> As of v4.6, `runStats` also carries Stage-2 judge rows (judge-tagged), so totals read higher than
> pre-4.6 runs for the same bench; anything keying `runStats` by model should exclude `role: 'judge'`.

Rewrite it: totals read higher **again** as of v4.7 — repairs, failed chair attempts and superseded
legs now get rows — and the correct rule is an **allowlist**, not a judge exclusion. Keep only
`seat`, `critic`, `lens:*`, `chair`, `claude`, `council`, `redteam`; drop everything else
(`src/council/ledger.js:49` `LEDGER_JOIN_ROLES`, plus the `lens:*` prefix rule at `:50-53`).

**Point at `docs/council.md`'s runStats role roster rather than restating the list a third time** —
`docs/council.md:89-93` and `:582-588` already carry it correctly, and
`skills/second-opinion/MANUAL-ORCHESTRATION.md:149-158` already carries a "⚠️ v4.7 CA-4 note". This
file is the **last** one on the old rule.

⚠️ `tests/model-notes-shipped-docs.test.js` pins section headings and three global rules. It has no
`judge`/`runStats` assertion, so this edit is free — but run it.

- [ ] **Step 2: MUST-6 — `docs/doc-system.md` documents a file that cannot exist**

Three places, all verified:
- `## Plans Index` (`:56-63`) documents `docs/plans/index.md` as auto-generated from `docs/plans/`
  and `docs/archive/plans/`. **Both directories do not exist** (`ls -d` → "No such file or
  directory"); the 66 real plans live in `docs/superpowers/plans/`. `buildPlansIndex`
  (`scripts/generate-docs.js:99-101`) scans the two absent dirs and `runWriteMode` (`:222-225`)
  writes the index only `if (fs.existsSync(path.dirname(plansIndexPath)))` — a permanent no-op. The
  doc also promises "the filename, first heading, and date"; the function emits only
  `- [name](docs/plans/name)`.
- `:68` — the Commands block comment says "Regenerate all auto sections + plans index".
- `:92` — a troubleshooting row: "Plans index missing | Run the script; it creates
  `docs/plans/index.md`". **That instruction cannot work.**

Rewrite the section to state plainly that no plans index is generated today and that plans live in
`docs/superpowers/plans/`; drop "+ plans index" from `:68`; delete the `:92` row.

⚠️ This file contains `<!-- AUTO:… -->` strings inside **fenced examples**. They are illustrations,
not live markers — do not treat them as generated content, and do not remove the fences.

- [ ] **Step 3: Verify and commit**

```
npx jest tests/model-notes-shipped-docs.test.js
node scripts/validate-docs.js --full
node scripts/generate-docs.js --check
```
Both scripts must exit 0. (`doc-system.md` is pinned by no test — `grep -rln doc-system tests/`
returns zero files — so the scripts are your only automated check here. Re-read your own edit.)

```bash
git add skills/second-opinion/MODEL-NOTES.md docs/doc-system.md
git commit -m "docs: retire the judge-exclusion rule and the phantom plans index"
```

---

## Task 5: `docs/ROADMAP.md` — resolve the live self-contradiction

**Files:**
- Modify: `docs/ROADMAP.md` (382 lines)

This file is pinned by **no test anywhere**, which is why it drifted. Nothing but you will catch an
error here.

- [ ] **Step 1: 8a — the Status paragraph contradicts the file's own v4.7 heading**

Lines `:16` and `:19-22` currently say (verified verbatim): *"Amicus is at **v4.6.3** (tagged
2026-08-05)"* … *"**v4.7 is not locked.** The composition scope below carried the number v4.6 here …
it remains the leading candidate — but it is a candidate, not a commitment."*

That is false **today**, independent of the release: the same file's `:179` heading already declares
the rescoped, locked, code-complete **"v4.7 — The count is the count"** scope, and composition was
moved out to its own unscheduled section (`:332`, "dropped from v4.7, 2026-08-05").

Rewrite the Status paragraph so v4.7 is named as the **locked, code-complete, unreleased** rev, and
composition is described as an unscheduled candidate for the next rev, to be decided at kickoff per
the anti-rot rule.

⚠️ **Do NOT write that v4.7 has shipped, and do NOT add a `— ✅ SHIPPED` marker to `:179`.**
`package.json` is 4.6.3 and there is no tag. The missing marker is *correct* right now; the release
cut adds it. Keep `:16`'s "Amicus is at **v4.6.3**" as-is for the same reason.

- [ ] **Step 2: 8b — an answered question still posed as open**

Lines `:204-208` say (verified verbatim): *"**Open kickoff question, do not assume either way:**
`chairAttempts[]` carries `{waveId, model, outcome, reason}` and **no usage**, so whether
failed-chair *cost* reaches `runStats` is unverified. Check before scoping — it is either already
handled or a third row class."*

It was answered: `src/council/run-chair.js:155` emits `role: 'chair-attempt'`, and the roster in
`docs/council.md` documents it as a non-primary row. Delete the open-question clause and state the
answer in one sentence.

- [ ] **Step 3: Verify and commit**

Re-read both edited regions in full. Confirm the file no longer argues with itself: search for
`not locked`, `leading candidate`, and `Open kickoff question` and make sure each hit is either gone
or now true.

```bash
git add docs/ROADMAP.md
git commit -m "docs(roadmap): v4.7 is locked and code-complete; the chair-attempt question is answered"
```

---

## Task 6: README flag symmetry, and file what this PR is not doing

**Files:**
- Modify: `README.md` (623 lines)
- Modify: `BACKLOG.md`

- [ ] **Step 1: S-4 — README flag symmetry (clause-length only)**

⚠️ **Three "the README doesn't mention X" claims were formally REFUTED in recon** — everything is in
`docs/usage.md` and `README.md:459` explicitly delegates ("Full field-by-field docs … are in
docs/usage.md"). The README is a 623-line index, not a flag reference.

What survives is a cosmetic asymmetry: the rows around `:410` and `:459` name `--tag`, `--search`
and `--all` but not `--limit`. Add `--limit` alongside its siblings — **clause-length additions
only**, at roughly `:408`, `:409`, `:410`, `:418`, `:459` (locate by content).

**Do NOT add a tagging section to the README.** If this reads as more than four clauses, stop and
report it — the delegation is deliberate design, not an oversight.

- [ ] **Step 2: File the six items this PR deliberately excludes**

Add to `BACKLOG.md`. Each needs the reason it was excluded, so nobody re-derives it next rev. Take
the entry text from `.superpowers/sdd/v47-docs-recon-report.md` § 5 (F-1 … F-6) — it is written
verbatim there:
- **F-1** pin MCP tool *parameters* (only names are derived today);
- **F-2** derive the README command list from `bin/amicus.js`'s 21 `case` labels;
- **F-3** run `generate-docs --check` in CI or jest (today it rests on a self-healing pre-commit hook
  a `--no-verify` commit can bypass);
- **F-4** dead code in `scripts/validate-docs.js` (`extractSection`/`findFilesInSection`/`checkDrift`
  are exported and unit-tested but called by no execution path);
- **F-5** document the `routing.tier` cost-tier surface (a four-rev-old hole, shipped in v3.2.0 — an
  M, because it means documenting the whole feature);
- **F-6** generalise the council TOC/link gates to the rest of `docs/`.

Also file the two recon UNVERIFIED items as a short rider: `fanout --quiet`'s end-to-end suppression
was verified by reading, not by launching a wave; and the `(unattributed)` consequence of tag
non-inheritance was traced through code, not observed in a real ledger.

- [ ] **Step 3: Verify and commit**

```
npx jest tests/docs-command-coverage.test.js tests/docs-quick-sync.test.js
```

```bash
git add README.md BACKLOG.md
git commit -m "docs(readme): --limit symmetry; file the doc-gate hardening riders"
```

---

## Final gates (controller, after all tasks)

1. `npx jest --runInBand` → **≥ 507 suites / ≥ 6883 passing**, zero failures.
2. `node scripts/validate-docs.js --full` → "All markers are current.", exit 0.
3. `node scripts/generate-docs.js --check` → exit 0.
4. `node scripts/check-file-sizes.js --all` → exit 0 (only `src/cli.js` changed, and it is
   grandfather-exempt — confirm anyway).
5. `npm run lint` → exit 0. **Not** bare `npx eslint .`, which sweeps ~247 pre-existing violations in
   ungated directories and is not the gate.
6. Whole-branch review, then ONE consolidated fix wave.
7. Push (allow ≥ 5 min — the pre-push hook runs the full suite), open the PR with the
   **`council-review`** label.

## Self-review notes

- **Coverage:** MUST-1 (T1), MUST-2a/2b (T2), MUST-3 (T3), MUST-4 (T2), MUST-5 (T4), MUST-6 (T4),
  MUST-7 (T2), ROADMAP 8a/8b (T5), S-1/S-2/`--quiet` (T1), S-3 (T3), S-4 (T6), F-1…F-6 filed (T6).
- **Deliberately excluded:** the two `"version": "4.6.3"` examples and the ROADMAP `SHIPPED` record —
  both belong to the release cut, so that every commit is true at the moment it lands.
- **The one source edit** is the `--quiet` help line in `src/cli.js`, owner-ruled 2026-08-08.
- Line numbers here were verified at `b365e03` but **locate every edit by quoted content** — an
  earlier task in the same file will shift them.
