# #257 — every remaining reader treats a promoted leg as no deliverable — Implementation Plan (addendum, owner decision C)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** the Stage-1 and Stage-2 repair solos, the chair attempt and its classifier, the debate defence and re-vote legs, and the two census side-predicates all treat a leg with `promoted: true` as no deliverable, each riding its existing not-ok path, so no reader in the council consumes promoted reasoning.

**Architecture:** one conjunct per site, `&& !isPromotedLeg(<leg>)`, from the existing leaf `src/council/promoted.js`; the chair carries the cause in `chairAttempts[].reason` (closed `outcome` enum untouched); debate gates the PARSE and keeps the real leg document so the rows carry `promoted: true`; no new degrade channel; the substitution chain is unchanged by measurement.

**Tech Stack:** Node.js CommonJS, Jest via `npm test` (never a jest pattern override on the command line; single files only through the ledger's `run-one.js`), the 300-line gate (`scripts/check-file-sizes.js`), the one-voice degrade contract.

**Spec:** `docs/superpowers/specs/2026-09-19-257-promoted-reasoning-design.md` §10 (rulings R-X21–R-X25). Measured map: the SDD ledger's `r10-sites-map.md` (every anchor at `4a2c111c`).

## Global Constraints

- **Baseline:** every line number below was measured on `4a2c111c`. Before editing, grep the QUOTED anchor text and confirm every identifier a new block reads is declared ABOVE the insertion line. If an anchor is not where this plan says, STOP and report NEEDS_CONTEXT — do not improvise.
- **Literal-true discipline:** the only truth test is `isPromotedLeg(leg)` from `./promoted` (a leaf, require-free; safe to require from every file here).
- **300-line gate:** `src/council/run-debate-revote.js` is at EXACTLY 300 — every edit in place; its `./promoted` require shares an existing require line. `run-stage2.js` 293, `run-debate.js` 290, `run-stages.js` 289, `run-chair.js` 243, `chair-fallback.js` 95, `verdict-seats-reviewed.js` 134.
- **Wording byte-identity:** no existing announcement template changes; every existing expected string in every test file stays untouched. The `debate-degraded` `why` is pinned in `tests/council/degrade-channels.test.js` and does NOT change (R-X23).
- **No new degrade channel** (R-X21/R-X23). The drift pin `tests/council/degrade-contract.test.js:243-260` reads every `channel:` literal in src — you add none.
- **Schemas:** `chairAttempts[].outcome` is a closed enum (`schemas/council-run.schema.json:134`) — reuse `'no-output'`; `findings[].debate.action` is closed (`schemas/council-tally.schema.json:94`) — reuse `'no-response'`. A debate `errors[].code` value `'REASONING_ONLY'` is introduced: grep `schemas/` and `tests/` for the existing codes (`DEAD_LEG`) first; if a schema or test closes that set, extend it in the same commit.
- **Commits:** one per task, `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` trailer, `(#257)` in the subject, NEVER a closing keyword before an issue number.
- **Named mutants:** each is an exact edit; after every hardening re-run the pin that guards it and confirm it still FAILS its mutant.
- **Tests run:** `node <ledger>/run-one.js <worktree> <files…>`; never a jest flag; the full suite is the controller's per wave.

---

### Task 11: the two repair solos

**Files:**
- Modify: `src/council/run-stages.js:214` (anchor `      const repaired = (solo.leg && solo.leg.summary) || '';`) and its requires (`:30` imports `reasoningOnlyClause, promotedFacts` from `./run-retry-notes`, which does NOT re-export `isPromotedLeg`).
- Modify: `src/council/run-stage2.js:225` (anchor `      const out = (solo.leg && solo.leg.summary) || '';`); it already requires `./promoted` at `:35`.
- Test: `tests/council/run-stages.test.js` — Stage-1 scenarios go in the `runStage1` repair region (beside the LC-6 trio `:221/:238/:259`, the "repair whose leg came back empty" test); Stage-2 scenarios beside the LC-12 trio `:2365-2411`.

**Interfaces:**
- Consumes: `isPromotedLeg` (Task 1 of the main plan).
- Produces: a promoted `-p<N>` repair leg is a failed repair attempt (`repaired === ''`; `conformance: 'unstructured'` after the bound; the `role: 'repair'` row carries `promoted: true`, minted by `buildRunStatsEntry`); a promoted `-q<N>` repair leg is never parsed (`out === ''`).

- [ ] **Step 1: Write the failing tests** (copy the harness of the neighbouring repair tests; `launchSolo` derives `leg` from `r.wave.legs[0]`, so script a solo whose leg is `{ ...mkLeg(model, 'Let me carefully analyze…'), promoted: true, usage: { tokens: { reasoning: 40332, output: 1 } }, finish: 'stop' }`):
  - Stage 1: `a promoted -p1 repair is a failed repair attempt: its reasoning is never validated as findings, the seat ends unstructured, and the repair row says promoted: true (#257 R-X21)` — the seat's review had no findings block; `-p1` returns the promoted leg; `-p2` returns a dead leg → assert `res.conformance`/the seat's `findings: []`, `extraRows` has two `role: 'repair'` rows, the first with `promoted === true`, and `launchSolo` was called exactly twice (the bound). Then the positive control: `-p1` promoted, `-p2` a clean JSON block → `conformance: 'repaired'` (the bound still allows the second attempt to rescue).
  - Stage 2: `a promoted -q1 repair supplies nothing: its block is never parsed (#257 R-X21)` — a NON-promoted judge with no block; `-q1` returns a promoted leg whose summary CONTAINS a valid fenced block (the adversarial case — it must NOT be used); `-q2` dead → `judgeResults[i].ok === false`, `fromReasoning === false` (the original was not promoted), the `-q1` repair row `promoted === true`, and NO `judge-reasoning-only` note. Then: original promoted + `-q1` NON-promoted clean → `ok: true` and the note's repair arm (`data.repaired === true`) — the existing `#257 (b2)` pin already covers this; reference it, do not duplicate.
  - Named mutants: **REPAIRPROMOTEDUSED** (drop the conjunct at run-stages.js:214) reds the Stage-1 test (the promoted reasoning would be "validated" — assert the negative via `findings: []` AND, more directly, that `repairing` never became the reasoning: the second repair's prompt/briefing must not contain `Let me carefully analyze` — read how the LC-6 tests observe `repairing`); **JUDGEREPAIRPROMOTEDUSED** (drop the conjunct at run-stage2.js:225) reds the Stage-2 adversarial test (`ok` would flip to true).
- [ ] **Step 2: Run the file — expect the new tests to FAIL.**
- [ ] **Step 3: Implement** — `run-stages.js`: add `const { isPromotedLeg } = require('./promoted');` beside the requires (289 → 290) and change `:214` to `      const repaired = (solo.leg && !isPromotedLeg(solo.leg) && solo.leg.summary) || ''; // #257 R-X21: a promoted repair is a failed attempt — its reasoning is not findings (named mutant "REPAIRPROMOTEDUSED", tests/council/run-stages.test.js)`. `run-stage2.js:225`: `      const out = (solo.leg && !isPromotedLeg(solo.leg) && solo.leg.summary) || ''; // #257 R-X21: a promoted repair supplies no block (named mutant "JUDGEREPAIRPROMOTEDUSED")`. Extend the `#202`/`#257` comment above the Stage-2 repair loop with one sentence: the `judge-reasoning-only` note's repair arm fires only for a promoted ORIGINAL rescued by a non-promoted repair, because a promoted repair can never supply the block.
- [ ] **Step 4: Run `tests/council/run-stages.test.js tests/council/run-stage2-notes.test.js tests/council/degrade-contract.test.js` — PASS; mutants red; `npm run lint`; `npm run check:sizes` (run-stages.js 290, run-stage2.js 293).**
- [ ] **Step 5: Commit** — `feat(council): a promoted repair solo is a failed repair attempt — its reasoning is never validated as findings nor parsed as a judgement (#257)`.

---

### Task 12: the chair attempt and its classifier

**Files:**
- Modify: `src/council/run-chair.js:61-62` (anchor `    const ok = solo.leg && solo.leg.status === 'complete'` / `      && solo.leg.summary && solo.leg.summary.trim();`) + a require of `./promoted`.
- Modify: `src/council/chair-fallback.js:87-90` (anchor `  if (rawLeg.status === 'complete') {`) + a require of `./promoted`.
- Test: `tests/council/run-chair.test.js` (the `chair retry + fallback promotion` describe `:167-220` and `chairAttempts[] recording (LC-5)` `:221-306`; harness `tests/council/helpers/fake-launchers.js` — a waveId→responder script, `run.json`/`verdict.json` read off disk).

**Interfaces:**
- Produces: `attemptChair` nulls a promoted chair leg (the walk continues: ch2 same chair, ch3 ledger-promoted fallback, give-up → `chair-failed`); `classifyChairAttempt(rawLeg)` returns `{ outcome: 'no-output', reason: 'answered only in its reasoning channel (<r> reasoning / <o> output tokens[, finish \'<f>\']); no synthesis to read' }` for a promoted complete leg; the `chair-attempt` row carries `promoted: true`.

- [ ] **Step 1: Write the failing tests:**
  - `a promoted ch1 chair is no synthesis: ch2 (same chair) is tried and, when it answers, exit is unaffected; chairAttempts[0] records no-output with the reasoning-channel reason (#257 R-X22)` — script ch1 as a promoted leg (summary `Let me carefully analyze…`, `promoted: true`, `usage.tokens {reasoning: 40332, output: 1}`, `finish: 'stop'`), ch2 clean → assert `verdict.overallVerdict` non-null, exit 0 (when nothing else degrades), `run.json.chairAttempts` equals `[{ waveId: '<runId>-ch1', model: '<chair>', outcome: 'no-output', reason: "answered only in its reasoning channel (40332 reasoning / 1 output tokens, finish 'stop'); no synthesis to read" }]`, and the `chair-attempt` runStats row for ch1 has `promoted === true` and `status: 'complete'`.
  - `three promoted chair legs give up: chair-failed names the reason for every attempt, overallVerdict is null, exit 2` — ch1, ch2, ch3 all promoted → the `chair-failed` `why` contains `ch1 <chair>: answered only in its reasoning channel` three times (once per attempt/model); `overallVerdict === null`; exit 2.
  - Unit: `classifyChairAttempt` on a promoted complete leg → the exact object above; on a promoted leg with no usage/finish → `(0 reasoning / 0 output tokens); no synthesis to read`; on a non-promoted complete leg with text → `{ outcome: 'completed', reason: null }` (byte-identical control).
  - Named mutants: **CHAIRPROMOTEDUSED** (drop the conjunct in `attemptChair`) reds the first test (the promoted reasoning becomes the synthesis, `chairAttempts` is empty); **CHAIRREASONDROPPED** (return `reason: null` for the promoted arm) reds the classifier unit and the `why` assertion.
- [ ] **Step 2: Run — expect FAIL.**
- [ ] **Step 3: Implement** — `run-chair.js`: `const { isPromotedLeg } = require('./promoted');` beside the requires; `:61` becomes `    const ok = solo.leg && solo.leg.status === 'complete' && !isPromotedLeg(solo.leg) // #257 R-X22: a promoted chair is no synthesis; the walk continues (named mutant "CHAIRPROMOTEDUSED", tests/council/run-chair.test.js)` with `:62` unchanged. `chair-fallback.js`: `const { isPromotedLeg, promotedFacts } = require('./promoted');`; inside the `status === 'complete'` arm, BEFORE `hasOutput`:
  ```js
    if (isPromotedLeg(rawLeg)) {
      // #257 R-X22: the closed `outcome` enum is untouched; the cause rides `reason`, which the
      // chair-failed `why` prints verbatim ("ch1 <model>: <reason>"). Named mutant "CHAIRREASONDROPPED".
      const f = promotedFacts(rawLeg);
      const finish = f.finish ? `, finish '${f.finish}'` : '';
      return { outcome: 'no-output',
        reason: `answered only in its reasoning channel (${f.reasoning} reasoning / ${f.output} output tokens${finish}); no synthesis to read` };
    }
  ```
  Update the classifier's docblock taxonomy sentence (`:79`) to say `'no-output'` also covers a promoted leg, with the reason.
- [ ] **Step 4: Run `tests/council/run-chair.test.js tests/council/chair-fallback.test.js tests/council/run-chair-seam.test.js tests/council/degrade-channels.test.js tests/council/verdict-degrades.test.js` — PASS; mutants red; `npm run lint`; `npm run check:sizes`.**
- [ ] **Step 5: Commit** — `feat(council): a promoted chair leg is no synthesis — the walk retries and falls back, and the attempt records why (#257)`.

---

### Task 13: debate defences and re-votes

**Files:**
- Modify: `src/council/run-debate.js:61-67` and `:84-88` + a require of `./promoted` (290 → 291).
- Modify: `src/council/run-debate-revote.js:172-174` and `:240-245` — the file is at EXACTLY 300: in-place edits only; the `./promoted` require goes ON THE SAME LINE as an existing single-line leaf require (e.g. `const { isPromotedLeg } = require('./promoted'); const { … } = require('./run-stats-entry');`), and the module docblock's allowed-requires list (`:23-27`/`:36-52`) gains `./promoted` by editing an existing line, never adding one. `wc -l` must read 300 afterwards.
- Check first: grep `schemas/` and `tests/` for `DEAD_LEG` — if any schema or test closes the `errors[].code` set, extend it with `REASONING_ONLY` in this commit.
- Tests: `tests/council/run-debate.test.js` (the dead-defence describe `:537-568`, the bounded-repair describe `:570-620`, the re-vote repair describes `:621-738`; harness `ctxFor` + `fake-launchers.js`, real `tally()`/`applyDebate()`), `tests/council/debate.test.js` if `allNoResponse` gains a caller shape.

**Interfaces:**
- Produces: a promoted defence leg → `parsed = { ok: false, byId: allNoResponse(expectedIds), errors: [{ code: 'REASONING_ONLY', detail: 'answered only in its reasoning channel' }] }`, `conformance: 'unstructured'`, the REAL leg kept (row `rebuttal` with `promoted: true`), one bounded repair; a promoted repair leg (`leg2`) is unparseable the same way and still supersedes as today; every bundled finding's original stands (`action: 'no-response'`). A promoted re-vote leg → `parsed = { ok: false, byId: {}, errors: [{ code: 'REASONING_ONLY', detail: 'answered only in its reasoning channel' }] }`, `conformance: 'unstructured'`, ONE bounded repair (`repairRevoteLeg`), a promoted repair unparseable the same way; the judge's provisional verdict stands.

- [ ] **Step 1: Write the failing tests:**
  - `a promoted defence leaves every original standing (no-response), keeps its leg on the row with promoted: true, is repaired once, and degrades the round (#257 R-X23)` — script `-d1` as a promoted leg whose summary CONTAINS a parseable defence (the adversarial case: it must not be applied), `-d1r` dead → `noResponse === expectedIds.length`, every finding `action: 'no-response'`, `degraded === true`, the `rebuttal` row for `-d1` has `promoted === true` and `status: 'complete'`, exactly one repair launched.
  - `a promoted defence whose repair answers cleanly is applied (the repair is the retry)` — `-d1` promoted, `-d1r` clean → the repaired defence applies, `conformance: 'repaired'`, the `-d1` row is `superseded` with `promoted === true`.
  - `a promoted re-vote is not applied, is repaired once, and a promoted repair still is not` — the re-vote wave's leg for one judge promoted with a parseable re-vote inside (adversarial), its repair promoted too → the judge's ORIGINAL Stage-2 verdict stands, `conformance: 'unstructured'`, round degraded, exactly one repair; then the clean-repair twin → `'repaired'` and applied.
  - Named mutants: **DEFENSEPROMOTEDUSED** (drop the conjunct at `:65`) reds the first; **DEFENSEREPAIRPROMOTEDUSED** (drop it at `:85`) reds a variant where `-d1` is dead and `-d1r` is promoted-with-parseable-defence; **REVOTEPROMOTEDUSED** (`:242`) and **REVOTEREPAIRPROMOTEDUSED** (`:173`) red the re-vote tests.
- [ ] **Step 2: Run — expect FAIL.**
- [ ] **Step 3: Implement** — `run-debate.js`: `:65-67` become
  ```js
  const usable = leg && !isPromotedLeg(leg); // #257 R-X23: a promoted defence is UNPARSEABLE — the leg document is kept for its row (named mutant "DEFENSEPROMOTEDUSED", tests/council/run-debate.test.js)
  let parsed = usable ? parseDebateDefense(leg.summary, expectedIds)
    : { ok: false, byId: allNoResponse(expectedIds), errors: [{ code: leg ? 'REASONING_ONLY' : 'DEAD_LEG', detail: leg ? 'answered only in its reasoning channel' : 'no summary' }] };
  let conformance = usable ? 'clean' : 'unstructured';
  ```
  and `:85` `    parsed = leg2 && !isPromotedLeg(leg2) ? parseDebateDefense(leg2.summary, expectedIds) : parsed; // #257 R-X23 (named mutant "DEFENSEREPAIRPROMOTEDUSED")`. Read `:88-97` to confirm the shaped literal's `conformance` is what `bad()` reads and that a kept promoted leg produces `status: 'complete'` + `conformance: 'unstructured'` there. `run-debate-revote.js`: `:242` `    let parsed = alive && !isPromotedLeg(leg) ? parseRevote(leg.summary, expectedIds)` and `:243` `      : { ok: false, byId: {}, errors: [{ code: alive ? 'REASONING_ONLY' : 'DEAD_LEG', detail: alive ? 'answered only in its reasoning channel' : 'no summary' }] }; // #257 R-X23 (named mutant "REVOTEPROMOTEDUSED")`, `:244` `    let conformance = alive && !isPromotedLeg(leg) ? 'clean' : 'unstructured';`, `:173` `  parsed = leg2 && !isPromotedLeg(leg2) ? parseRevote(leg2.summary, expectedIds) : parsed; // #257 R-X23 (named mutant "REVOTEREPAIRPROMOTEDUSED")`. Line count stays 300.
- [ ] **Step 4: Run `tests/council/run-debate.test.js tests/council/debate.test.js tests/council/run-schema-debate.test.js tests/council/degrade-channels.test.js tests/council/report-debate.test.js tests/council/run-debate-stage.test.js` — PASS; mutants red; `npm run lint`; `npm run check:sizes` (run-debate-revote.js exactly 300).**
- [ ] **Step 5: Commit** — `feat(council): a promoted debate defence or re-vote is unparseable — the original stands, the leg keeps its row, and the one bounded repair is the retry (#257)`.

---

### Task 14: the two census side-predicates

**Files:**
- Modify: `src/council/verdict-seats-reviewed.js:77` (`isUnverifiedSeat`) and `:82` (`isRefusedSeat`) — each gains `&& r.promoted !== true`; extend the docblock at `:70` ("`unverified` can therefore never exceed `reviewed`") to say the predicates now guarantee it.
- Test: `tests/council/verdict.test.js` beside V19/V20.

- [ ] **Step 1: Write the failing tests:** a hand-assembled bench row with `status: 'complete', promoted: true, findingsUnverified: true` counts as neither `reviewed` nor `unverified` (`{ reviewed: 0, unverified: 0, refused: 0, of: 1 }`); the same with `repairRefused` → `refused: 0`. Named mutant **UNVERIFIEDPROMOTED** (delete the new conjunct in `isUnverifiedSeat`) reds the first; **REFUSEDPROMOTED** the second.
- [ ] **Step 2: Run — expect FAIL (unverified 1).**
- [ ] **Step 3: Implement** the two conjuncts with a one-line `// #257 R-X25` comment each.
- [ ] **Step 4: Run `tests/council/verdict.test.js tests/council/report-unverified.test.js` — PASS; mutants red; `npm run check:sizes`.**
- [ ] **Step 5: Commit** — `fix(council): the census's unverified and refused predicates exclude a promoted row too (#257)`.

---

### Task 15: docs and record

**Files:**
- Modify: `CHANGELOG.md` `[Unreleased]` — the #257 bullet's scope sentences: replace the judge sentence's neighbourhood with: "Every reader of a leg in the council now treats a promoted leg as no deliverable: Stage-1 reviews; the Stage-1 and Stage-2 repair solos (a promoted repair is a failed repair attempt inside the existing bound); the chair (its walk retries and falls back as for a chair with no output, and `chairAttempts[]` records `no-output` with the reason `answered only in its reasoning channel (…); no synthesis to read`); debate defences and re-votes (unparseable: the original stands as `no-response`, the leg keeps its row with `promoted: true`, the one bounded repair is the retry). The fallback-substitution chain is unchanged — a reasoning-only answer is not a capacity signal." Keep the upgrade note; add to it: "a chair, defence or re-vote that 4.13.0 accepted on a promoted answer is now retried or stands down".
- Modify: `docs/council.md` (the #257 paragraph in "Leg completion and `session.status`") and `docs/troubleshooting.md` (the #257 section: add the chair and debate symptoms — `chairAttempts[]` `no-output` with the reason; a defence `no-response`).
- Modify: `BACKLOG.md` `## #257 — filed at the PR`: the R10 line becomes a record that decision C pulled every listed reader in at council round 1 (2026-09-19) except the substitution chain (R-X24, measured: `isRetryable` is capacity-only); add: "`src/sidecar/fanout-output.js:27` — the `amicus fanout` CLI prints a promoted leg's reasoning as its answer with no marker (R1's sibling)".
- Spec: §10 is already committed; nothing else.
- Gates: the five docs gates via run-one.js; `npm run check:citations`; `node scripts/generate-docs.js --check`; then the FULL `npm test` from the worktree (record the counts verbatim).
- [ ] Commit — `docs(council): every council reader stands a promoted leg down — CHANGELOG scope, council.md, troubleshooting, BACKLOG (#257)`.
