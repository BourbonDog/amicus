# v4.8.0 · T2.4 / PR C — the consumers

**Branch** `v48-t24-consumers` · **BASE** `ed5c0c02` (merge of PR #174) · written 2026-08-20.

Closes Phase 2. T2.3 gave the producer a way to say *"a vote exists that I cannot attribute"*
(`unattributedPeerDrops`, announced in both documents). Nothing renders it. T2.4 is the consumer
half: make the vote→column join **refuse** a key that identifies nothing, and give the vote a place
to appear.

Written just-in-time per the standing ruling on plan rot. Every number below was measured at BASE on
2026-08-20 and must be **re-measured** by the task that depends on it.

---

## 0. Measured substrate — 2026-08-20 at `ed5c0c02`

### 0.1 Gates

All four exit 0. Full suite: **541 suites / 7666 passed / 8 skipped / 0 failed**, 4 snapshots
passed. No flake in this run.

### 0.2 Sizes (authoritative rule: `content.split('\n').length`, −1 if it ends with `\n`)

| File | Lines | |
|---|---:|---|
| `src/council/report.js` | 197 | the work lands here |
| `src/council/report-html.js` | 146 | |
| `src/council/report-md.js` | 130 | |
| `src/workspace/matrix-model.js` | 129 | the work lands here |
| `electron/workspace-ui/workspace-matrix.js` | 260 | third renderer |
| `src/council/verdict.js` | 231 | |
| `src/council/run-retry.js` | **299/300** | ⛔ untouchable |
| `src/council/run-stages.js` | **294/300** | ⛔ untouchable |

⚠️ **The ceiling is the risk, not the logic.** This repo's comment style inflates a file fast:
`peer-split.js` measured 67 → 165 → 218 → 244 → 266 → 282 → 289 across one PR, and a
*pure-documentation* round in it cost 7 lines. `report.js` has 103 lines of headroom and this change
is comment-worthy. Budget it. **If the gate fires, EXTRACT — never shave comments** (Constraint 6).

### 0.3 The two joins, measured by probe

`src/council/report.js :: toModel` builds `byJudge` with
`byJudge[(seatSpace && adj.seat) || adj.judge] = adj.verdict`. Every unidentifying or orphaned key
lands as a junk entry that no column reads:

| Case | resulting `byJudge` key | read by a column? |
|---|---|---|
| A · seat space, orphan seat `deepseek#9` | `"deepseek#9"` | no |
| B · alias space, `judge: ''` | `""` | no |
| C · alias space, `judge: undefined` | `"undefined"` ← JS coercion | no |
| D · seat space, seat falsy + `judge: ''` | `""` | no |
| E · seat space, seat falsy + `judge` undefined | `"undefined"` | no |

`src/workspace/matrix-model.js :: buildMatrixModel` runs the same expression at `:84` behind a
`typeof adj.judge !== 'string'` guard (`:83`). Measured consequence: it **refuses** an undefined
judge outright, but `''` is a string so it still writes a junk key. Its `votes` map is internal and
cells are read by column key, so the junk never escapes into the model.

**So the two joins sit at different strictness levels today.** Noted; unifying them via an
extraction is explicitly **out of scope** (ruling R17 took the narrow option). Both must satisfy the
same rule, written twice, not shared.

### 0.4 The renderers need ZERO edits — measured, and load-bearing

Probe at BASE: appending `'UNATTRIBUTED'` to `model.judges` and keying the vote under it produced,
with **no renderer change at all**:

```
renderMd header : | Finding | Sev | Raiser | deepseek | gpt | UNATTRIBUTED | Tier | Decision |
renderMd row    : | F1 | major | gpt | ✓ |  * | ✗ | Contested |  |
renderHtml      : <th>UNATTRIBUTED</th> present; cells ✓ and ✗
```

`report-md.js:49` and `report-html.js:41` both iterate `m.judges` and read `f.byJudge[j]`.
`electron/workspace-ui/workspace-matrix.js:62,72` iterates `matrix.judges` and `row.cells` the same
way.

⚠️ **Read R3's "three renderers move together" correctly.** It means they move together *because
they share the model*, not that three files need edits. **Do not edit a renderer.** If you believe
one needs a change, that is a finding — stop and report it rather than editing.

### 0.5 The pins that assert the WRONG behaviour

`tests/council/seat-matrix.test.js:289` — `describe('T22: the two orphaned-seat shapes, pinned as
DISCLOSED behaviour (§4.6)')`. Its own comment at `:313-315` says: *"A future fix must edit this line
deliberately, not discover the gap in production."* That is this PR.

- **T22 shape 1** (`:307`, judge's Stage-2 seat orphaned) — measured today:
  `verdict.findings[0].basis` is `{a:1, d:1, n:0}` while `renderedBasis(...)` is `{a:1, d:0, n:0}`.
  The orphaned dispute is counted and rendered nowhere. **This pin must be REPLACED**, not adjusted
  — the PR B lesson: an adjusted pin can go green against its own mutant.
- **T22 shape 2** (`:337`, the RAISER's Stage-1 seat orphaned) — a *different* shape. Both votes are
  properly seated; only `raiserSeat` is missing, so the star disappears. R3 governs an **orphaned
  Stage-2 judge vote**; this is Stage-1 raiser attribution. **Out of scope.** Its row must come out
  **unchanged** — verify by execution, do not assume.

### 0.6 Blast radius of the column

8 assertions of the string `Finding | Sev | Raiser` across `tests/council/report-claude-column.test.js`
and `tests/council/seat-matrix.test.js`, plus 2 snapshot files
(`__snapshots__/report-claude-column.test.js.snap`, `__snapshots__/report-debate.test.js.snap`).

Small — **provided the column is conditional**. See §2 R18.

### 0.7 `basis` is not computed here

`toModel` copies `basis: f.basis || {a:0,d:0,n:0}` straight through from the tally. R3's "keep it in
basis" therefore needs **no code** — it needs a pin proving nothing moved it.

---

## 1. Scope

Two items. Nothing else.

1. **SI-22.5** (⚠️ **written as "SI-12" throughout this plan — wrong, corrected by ruling R19 on
   2026-08-20; SI-12 is row `| 12 |`, double-orphan conformance collapse, and is still OPEN**)
   — the vote→column join must **refuse** a key that identifies nothing (`''`, `undefined`,
   and any non-string) instead of writing a junk entry. Both joins.
2. **SI-22.5** — a vote that cannot be attributed to a column renders in an **UNATTRIBUTED** column,
   with the vote still counted in `basis`.

⚠️ **SI-12 had no filing.** Its entire prior specification was one clause. The scope above is the
owner's confirmed reconstruction (2026-08-20, ruling R17) — record it as a filing so the next reader
inherits a definition instead of a phrase.
> ⚠️ **Annotated 2026-08-20 (T-C3 fix round 1): this paragraph is FALSE and ruling R19 replaces
> it.** SI-12 *does* have a filing and always did — row `| 12 |` of the live status table in
> `2026-08-16-v48-phasing-and-rulings.md` (*double-orphan conformance collapse*,
> `run.js :: runCouncil`) and its `- [ ]` entry in `BACKLOG.md`, **still OPEN**. The search that
> "found nothing" was `git grep SI-12`, and that table writes a bare `| 12 |`, so the string never
> occurs — a search that could not express its target reported nothing wrong. **R19: the work this
> plan describes is SI-22.5. Fold it there; do not mint a new identifier; leave SI-12 alone.**

---

## 2. Owner rulings — new, 2026-08-20

Append to the rulings table in `docs/superpowers/plans/2026-08-16-v48-phasing-and-rulings.md`
(§4 runs R1–R16; these are **R17** and **R18**). Append — do not renumber anything.

| # | Question | Ruling |
|---|---|---|
| R17 | What is the consumer-side unidentifying-key defect? (asked as *"what is SI-12?"* — ⚠️ wrong label, corrected by **R19**) | **The consumer-side sibling of PR B's council C1** — `report.js :: toModel` and `matrix-model.js :: buildMatrixModel` accept `''`/`undefined` as a column key and write a junk entry; the join must refuse. **Narrow option taken:** the report.js/matrix-model.js strictness asymmetry is NOT in scope, and no shared module is extracted |
| R18 | Where does a refused vote go? | **Fold into UNATTRIBUTED** — one column, one concept. An orphaned seat and a non-identity both land there; the vote stays in `basis` |

R18 is the R2 rule (*"where the producer genuinely has nothing, mark explicitly, attribute
nothing"*) applied at the consumer. R3 already governs the orphan half.

---

## 3. Global Constraints

**Inherited verbatim from `docs/superpowers/plans/2026-08-17-v48-t2x-run-retry-extraction.md` §Global
Constraints.** They were earned by ~30 review findings. Not re-derived, not softened. Read that
section — 1–11 and 5a–5d bind every task here. The ones this PR will actually collide with:

- **1 · Verification by EXECUTION, never assertion.** Every number in this plan is measured; every
  number you write must be too.
- **2 · Preservation pins are green at HEAD by construction — prove each with a NAMED MUTANT, never
  RED-before-GREEN.** The behaviour changes here (the refusal, the column) DO get RED-before-GREEN.
- **3 · NEVER `git checkout -- <path>`.**
- **5a/5b · Sweep citations of every file your commit TOUCHES. Symbol anchors, never new line
  numbers, never offset arithmetic — OPEN THE LINE.** Whole `file.js :: symbol` token on ONE physical
  line, or `check-citations.js` is blind to it.
- **5c/5d · Sweep for prose the behaviour change falsified, and read every NEW sentence against the
  code.** `docs/council.md` is covered by no gate at all.
- **6 · The 300-line gate blocks the COMMIT. EXTRACT, never shave.**
- **7 · No test whose title claims more than its assertion executes.**
- **10 · The council RENUMBERS findings between rounds — anchor by commit + mechanism, never id.**

Additional, specific to this PR:

- **C-1 · The column is CONDITIONAL.** `UNATTRIBUTED` appears in `judges` only when the document
  actually has an unattributable vote. Emitted always, it rewrites every existing report and breaks
  the byte-unchanged-artifact contract §0.6 depends on. Mirrors T2.3's emit-only-when-`> 0`.
- **C-2 · `basis` does not move** (R3). Pin it.
- **C-3 · Do not edit a renderer** (§0.4).
- **C-4 · `"undefined"` must never become a column label.** It is a JS coercion artifact, not a name.
- **C-5 · Nothing may claim SI-22.1 or SI-22.2 is closed.** R2 stands: the ambiguous *peer* drop
  still stays, `basis` still does not move there, the undercount deliberately remains.

### Gates — all four must exit 0 before the PR is opened

```
npm test                                  # baseline 541 suites / 7666 passed / 8 skipped / 0 failed
node scripts/check-file-sizes.js --all
npm run lint
npm run validate-docs
```

⚠️ `pre-push` blocks unless `.test-passed` matches HEAD exactly — run `npm test` AFTER the final
commit.
⚠️ Known intermittent: `tests/docs-plan-refs.test.js`, ~1 red in 8 runs, cause unknown. Capture the
full ● block before re-running.

---

## 4. Tasks

### T-C1 — `report.js`: the pre-pass, the refusal, the column

The core. `judges` is built at `report.js:89`, **before** the per-finding loop at `:98` where an
unattributable vote becomes detectable — so this is a **two-phase build**, not a one-line insert.

> ⚠️ **Citations annotated 2026-08-20 (v4.8 T-C3), not renumbered** — this plan is preserved dated
> record. Both were true at the branch point `ed5c0c02`, and the task this plan describes falsified
> them: at `e5376399` the roster build is `const bench = …` and the per-finding loop has moved, both
> inside `src/council/report.js :: toModel`, and the classification the plan called for now lives in
> that function's `columnFor`. The two-phase claim itself still holds and is what shipped.
> ⚠️ The `:98` here is a **bare `:NNN` continuation**, invisible to `check-citations.js` — see
> BACKLOG's citation-gate Mechanism C.

1. Classify each adjudication's key: identifying (a string that names a real column), orphaned (a
   string that names no column), or non-identifying (`''`, `undefined`, non-string).
2. Orphaned and non-identifying both route to `UNATTRIBUTED` (R18). Identifying keys behave exactly
   as at HEAD.
3. Append the column to `judges` only if at least one finding in the document routed a vote to it
   (C-1).
4. Replace T22 shape 1's pin deliberately (§0.5). Verify T22 shape 2 is **byte-unchanged**.
5. Prove `basis` did not move (C-2) and the renderers were not edited (C-3).

**RED-before-GREEN** for the refusal and the column. **Named mutants** for what must not regress —
at minimum one that removes the conditional so the column always appears, and one that reverts the
refusal to HEAD's expression. Record measured red sets **in the tree**, not in a report path.

### T-C2 — `matrix-model.js`: the same rule, written again

Same three-way classification at `:82-85`, same conditional column in `columns`/`judges`. Its
existing `typeof adj.judge !== 'string'` guard already refuses part of the domain — state precisely
what it does and does not cover before changing it, and do not delete a guard that is doing work.

Verify `electron/workspace-ui/workspace-matrix.js` needs zero edits (§0.4) by execution.

⚠️ Per R17 this is a **second implementation, not a shared one.** Do not extract a common module.

### T-C3 — the sweep

- ~~File **SI-12's definition** (§1)~~ **File it under SI-22.5** (ruling **R19**, 2026-08-20 — the
  `SI-12` label in this plan is wrong throughout; SI-12 is a different, still-OPEN defect) and
  **R17–R20** (§2) so the next reader inherits a spec.
- `docs/council.md` — document the column. Covered by no gate; read every new sentence against the
  code.
- `CHANGELOG.md`. State the baseline any byte-identity claim is true against — PR B shipped a
  born-false sentence by claiming release-scope byte-identity for a commit-scope measurement.
- **Inherited handoff 1:** `report.js`'s `byJudge[adj.judge]` citation rot — three live sites,
  re-derived 2026-08-19: `src/council/debate.js:89`, `src/council/run-debate.js:202`,
  `tests/council/debate.test.js:155`. Real expression is `byJudge[(seatSpace && adj.seat) || adj.judge]`.
  > ⚠️ **Annotated 2026-08-20 (T-C3): that "real expression" is itself no longer real.** It was true
  > at the branch point `ed5c0c02`; T-C1 replaced it with `byJudge[columnFor(adj)]`, and `columnFor`
  > is where the old expression is now computed **and then classified**. The three cited sites are
  > unchanged and still say `byJudge[adj.judge]`. `BACKLOG.md` carries the corrected form.
  ⚠️ **NOT a fourth site:** `tests/council/seat-matrix.test.js:75` uses the bare string deliberately,
  to name the pre-fix behaviour its fixture disproves. Re-derive all four before editing — this PR
  moves `report.js`.
- **Inherited handoff 2:** two stale `docs/council.md` citations, verified stale 2026-08-20 by
  opening the lines (the file is 1123 lines):
  > ⚠️ **Annotated 2026-08-20 (T-C3): the count "two" is wrong and was corrected the same day.**
  > There are **four distinct stale `docs/council.md` targets across six citing sites**; the two
  > named below are real, the count was not. Two of the six live in `src/` and T-C3 was forbidden
  > to edit them — see `BACKLOG.md`'s "Citations T2.4 measured but could not apply". Also:
  > `docs/usage.md:406` looks stale and is **CORRECT** — do not "fix" it.
  `tests/council/ledger.test.js@ed5c0c02:842` cites `:906-913` for the `amicus council tally` shape
  — that range is the "Worked example" heading and frontier-tier prose;
  `tests/council/report-claude-column.test.js@ed5c0c02:13` cites `:1002` for a `*` legend —
  `1000-1004` is a tier-count table. Re-anchor by symbol or section, never by a new line number.
  > ⚠️ **Both citing lines are pinned `@ed5c0c02` (2026-08-20, T2.4 fix round 1) and BOTH are now
  > history in two senses**: T2.4 shifted those test files, AND it **deleted the very citations
  > described here** — neither test comment carries a `docs/council.md:NNN` line citation any more,
  > because both were re-anchored to a named section. Read this bullet as the record of a fix, not
  > as a live description of either file.
- Close the T2.4 release record in the phasing doc (`✅ COMPLETED <date> by <task> (<sha>)` — follow
  the T2.2/T2.3 convention exactly) and repoint `BACKLOG.md`'s NEXT TASK at Phase 3.
- Final behavioural sweep (5c) + citation sweep of every touched file (5a).

---

## 5. Definition of done

- An unattributable vote renders in an `UNATTRIBUTED` column in the md report, the html report, and
  the workspace matrix — with **no renderer edited**.
- The column is absent from every document that has no such vote; the 4 snapshots and the 8 header
  assertions are unchanged except where deliberately replaced.
- `''`, `undefined` and non-string keys are refused by both joins; no `""` or `"undefined"` key
  survives into any model.
  > ⚠️ **Annotated 2026-08-20 (T-C3 fix round 1): the second clause is MEASURABLY FALSE as
  > written, and the shipped implementation narrows it deliberately.** True of the **join**: nothing
  > either `columnFor` writes is `""` or `"undefined"`, verified across every refusal class in both
  > consumers. **False of the model**, because the **roster** can seed such a key — `isSeatSpace`
  > accepts `{id: ''}`, so on a roster that itself holds `''`,
  > `toModel(…).judges` is `["", "gpt", "UNATTRIBUTED"]` and `byJudge` carries a `""` key. That
  > roster is the one T-C2 fix round 1 was about, so it is reachable, not hypothetical. The correct
  > property is **a statement about the JOIN, not about the model's whole key set**, and
  > `tests/council/seat-matrix.test.js` pins it in exactly those words.
- `basis` is unmoved, and pinned so.
- T22 shape 1 replaced deliberately; T22 shape 2 unchanged and proven so.
- ~~SI-12 has a filing.~~ **SI-22.5 has a filing** — ruling **R19** (2026-08-20): this work is
  SI-22.5, not SI-12, which is a different and still-OPEN defect (row `| 12 |`, double-orphan
  conformance collapse). R17/R18/R19/R20 recorded. T2.4 closed in the phasing doc.
- Four gates 0; `npm test` run after the final commit.
- Nothing claims SI-12, SI-22.1 or SI-22.2 is closed.
