// src/observe/watch-render.js
'use strict';

// Task 12 replaces this stub with the real renderers (table/plain/json + loop
// + exit codes). This exists only so cli-handlers-watch.js's require() of
// runWatchLoop resolves during Task 11 (a sanctioned stub-then-replace for a
// forward dependency); it must not gain any render logic here.
async function runWatchLoop() { return 0; }

module.exports = { runWatchLoop };
